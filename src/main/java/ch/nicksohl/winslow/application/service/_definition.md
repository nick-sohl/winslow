### Definition
- A services bundles multiple use cases
- If you write one StudentService with saveStudent(), deleteStudent(), findStudentById(), etc., you are basically bundling multiple use cases together.